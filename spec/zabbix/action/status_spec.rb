require 'spec_helper'

describe Zabbix::Action::Status do
  it 'has a version number' do
    expect(Zabbix::Action::Status::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
